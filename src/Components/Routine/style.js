import styled from "styled-components/native";

export const RoutineTitleContainer = styled.View`

`
export const RoutineTitle = styled.Text`
font-weight: 900;
`

export const RoutineDescription = styled.Text`
    margin: 5px 0;
`
